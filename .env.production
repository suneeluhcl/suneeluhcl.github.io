VITE_CHAT_API_URL=https://portfolio-chat-worker.suneeluhcl-eec.workers.dev

# Cloudflare Web Analytics beacon token.
# Get it at: Cloudflare dashboard -> Analytics & Logs -> Web Analytics -> Add a site
# (suneelkumarbikkasani.com) -> copy the value of `token` from the snippet it shows.
# The beacon is public by design, so committing it here is safe. Leave blank to
# ship no analytics at all.
VITE_CF_ANALYTICS_TOKEN=
