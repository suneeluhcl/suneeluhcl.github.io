{
  "name": "portfolio-chat-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "logs:summary": "node scripts/chat-log-summary.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11",
    "wrangler": "^4.120.0"
  }
}
